
enum
{
    MODE_VIEW = 1,
    MODE_SETUP
};

enum
{
    EXIT_MODE = 0
};

enum
{
    VIEW_DEVICEMGMT = 1,
    VIEW_MEDIA
};

enum
{
    DEVICE_INFORMATION = 1,
    DEVICE_DISCOVRY_MODE,
    DEVICE_CAPABILITIES,
    DEVICE_USER
};

enum
{
    MEDIA_SNAPSHOT_URI = 1,
    MEDIA_STREAM_URI,
    MEDIA_PROFILES
};

enum
{
    SETUP_USER = 1,
    SETUP_MEDIA 
};

enum
{
    ADD_NEW_USER = 1,
    DELETE_USER,
    SET_RESOLUTION,
    SET_FRAME_RATE,
    SET_MAX_BITRATE
};

enum
{
    MJPEG = 1,
    H_264,
    PLUGINFREE,
    MOBILE
};

enum
{
    Administrator = 1,
    Operator,
    User,
    Anonymous,
    Extended
};
